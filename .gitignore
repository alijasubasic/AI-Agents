# --- Secrets: never commit real credentials ---
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
token.json
service-account*.json

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
venv/
.venv/
env/

# --- Tooling caches ---
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# --- Agent run artifacts (traces contain prompt/response payloads) ---
traces/
*.trace.json
logs/
*.log

# --- Editors / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# --- Brain briefs (contain decision text) ---
briefs/

# --- Telemetry cache (counts from local Claude Code transcripts) ---
.cache/

# --- Lead exports and the do-not-contact list ---
# Both hold other people's contact details. Neither belongs in a repository,
# and the suppression list is per-deployment rather than per-clone.
leads/
suppression*.jsonl

# --- Obsidian vault written by the console demo ---
vault/
